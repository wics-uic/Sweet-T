---
const navOrderLinks = [
  "🍰 Cakes",
  "🎂 Custom cakes",
  "🧁 Cupcakes",
  "🧁✨ Custom cupcakes",
  "🥧 Pastelitos",
  "🍮 Flan",
  "🍪 Assorted cookies",
];
---

<div class="footer-container bg-green-100 py-8 m-[30px] flex flex-col items-center justify-center text-center rounded-t-[32px] rounded-b-[32px] p-[50px]">
  <!-- top content -->
  <div class="footer-items flex flex-col items-center gap-4">
    <img src="src/assets/Sweet_T_clear.svg" alt="Sweet T logo" class="w-24 sm:w-32" />

    <h2 class="quote text-xl font-semibold text-beige-50">No spam, just flan.</h2>
    <h3 class="email-statement text-sm text-beige-50 max-w-md mt-2">
      Just weekly menus, pickup reminders for your orders.
    </h3>

    <!-- Email signup -->
    <div class="email_input flex">
      <input
        id="email-input"
        type="email"
        placeholder="Email"
        class="p-3 w-125 rounded-l-xl bg-beige-50 border border-beige-50 text-gray-800 focus:outline-none"
      />
      <button
        class="bg-beige-50 text-[#013220] px-4 rounded-r-xl hover:bg-beige-200 transition flex items-center justify-center"
      >
        <img src="src/assets/arrow.svg" />
      </button>
    </div>
  </div>

  <!-- bottom footer links (responsive) -->
  <div class="w-full flex flex-col items-center gap-6 mt-10">
    <!-- Navigation links for food -->
    <div class="flex flex-wrap justify-center gap-6 text-beige-50 text-sm">
      {navOrderLinks.map((item) => (
        <a href="#" class="hover:underline">{item}</a>
      ))}
    </div>

    <!-- Navigation info links (always centered) -->
    <div class="flex flex-col gap-3 text-beige-50 text-sm items-center text-center">
      <a href="#" class="hover:underline">Contact us</a>
      <a href="#" class="hover:underline">FAQ</a>
    </div>
  </div>
</div>

<style>
  /* match the class name you used in the HTML */
  .email_input {
    background-image: url('arrow.svg');
  }
</style>
